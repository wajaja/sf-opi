<h1>Unliker list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Createdat</th>
            <th>Unlike</th>
            <th>Author</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for document in documents %}
        <tr>
            <td><a href="{{ path('unliker_show', { 'id': document.id }) }}">{{ document.id }}</a></td>
            <td>{% if document.createdAt %}{{ document.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{{ document.unlike }}</td>
            <td>{{ document.author }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('unliker_show', { 'id': document.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('unliker_edit', { 'id': document.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('unliker_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
