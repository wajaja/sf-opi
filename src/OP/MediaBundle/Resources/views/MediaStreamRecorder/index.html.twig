<h1>MediaStreamRecorder list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Path</th>
            <th>Size</th>
            <th>Type</th>
            <th>Createdat</th>
            <th>Updateat</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for document in documents %}
        <tr>
            <td><a href="{{ path('mediastreamrecorder_show', { 'id': document.id }) }}">{{ document.id }}</a></td>
            <td>{{ document.Path }}</td>
            <td>{{ document.size }}</td>
            <td>{{ document.type }}</td>
            <td>{% if document.createdAt %}{{ document.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{% if document.updateAt %}{{ document.updateAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('mediastreamrecorder_show', { 'id': document.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('mediastreamrecorder_edit', { 'id': document.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('mediastreamrecorder_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
