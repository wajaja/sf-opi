<h1>Picture list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Path</th>
            <th>Size</th>
            <th>Createdat</th>
            <th>Updateat</th>
            <th>Name</th>
            <th>Author</th>
            <th>Post</th>
            <th>Icomments</th>
            <th>Ilike</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for document in documents %}
        <tr>
            <td><a href="{{ path('picture_show', { 'id': document.id }) }}">{{ document.id }}</a></td>
            <td>{{ document.path }}</td>
            <td>{{ document.size }}</td>
            <td>{% if document.createdAt %}{{ document.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{% if document.updateAt %}{{ document.updateAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{{ document.name }}</td>
            <td>{{ document.author }}</td>
            <td>{{ document.post }}</td>
            <td>{{ document.iComments }}</td>
            <td>{{ document.iLike }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('picture_show', { 'id': document.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('picture_edit', { 'id': document.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('picture_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
